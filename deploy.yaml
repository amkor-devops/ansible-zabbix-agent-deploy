- hosts: all
  become: true
  vars:
    user:
      - name: "kube"
        password: "secret"
        ssh_key: "ssh-rsa …"

  tasks:
    - name: install packages
      become: true
      become_user: root
      dnf:
        state: present
        name:
          - tcsh
          - htop
